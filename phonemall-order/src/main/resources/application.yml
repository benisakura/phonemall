spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/phonemall_oms?characterEncoding=utf8&useSSL=false&serverTimezone=UTC&rewriteBatchedStatements=true

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto


server:
  port: 9000